import React from 'react';
import ReactDOM from 'react-dom';
import App from './App/App';

ReactDOM.render(
  <React.StrictMode>
    <App title="Evoush Landing"/>
  </React.StrictMode>,
  document.getElementById('root')
);
